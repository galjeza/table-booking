generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["referentialIntegrity"]
}

datasource db {
  provider     = "mysql"
  url          = "mysql://jh3met6qu25gmums8k7z:pscale_pw_r7yPbuTeLnewGfEBPiuxczioPvJFggXhWiMJSSUnIFH@aws-eu-west-2.connect.psdb.cloud/table-booking?sslaccept=strict"
  relationMode = "prisma"
}

model User {
  id    Int     @id @default(autoincrement())
  phone String  @unique
  email String  @unique
  name  String?
  lastName String?
  password String?
  role Role @default(USER)
}

model Restaurant {
  id    Int     @id @default(autoincrement())
  name String
  location  String?
}

enum Role{
    USER
    ADMIN
}